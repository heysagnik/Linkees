export interface IIcon{
    color?: string;
}
export interface ICard{
    children: React.ReactElement[];
    i: number;
    title: string;
    subtitle: string;
    link: string;
    cover: string;
}
