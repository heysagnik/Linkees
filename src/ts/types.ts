export type ThemeType = "dark" | "light";
export type ItemType = {
    title: string;
    subtitle: string;
    image: string;
    link: string;
}
export type SongType = {
    title: string;
    audio: string;
    image: string;
}